[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "pyChanga"
version = "1.0.0"
description = "A small Python music API with synchronized, independent live parts"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
pyChanga = "pyChanga.__main__:main"

[tool.setuptools]
packages = ["pyChanga", "pyChanga._vendor", "pyChanga._vendor.cloudpickle"]

[tool.setuptools.package-data]
pyChanga = ["sounds/pyChanga.sf2", "sounds/*.txt", "sounds/*.json"]
"pyChanga._vendor.cloudpickle" = ["LICENSE"]
